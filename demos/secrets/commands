kubectl create secret generic <secret-name> \
	--from-literal=<key>=<value> \
	--from-literal=<key>=<value>



kubectl create -f

kind: Secret
metadata:
 name: app-secret
data:
 DB_HOST: mysql
 DB_User: root
 DB_Pasword: asd

you need to hash the values
echo -n 'mysql' | base64 etc..
echo -n "whatever" | base64 --decode

kubectl get secrets
kubectl describe secrets
kubectl get secret app-secret -o yaml

envFrom:
 - secretRef:
   name: app-secret
