apiVersion: v1
kind: LimitRange
metadata:
  name: constrain
spec:
  limits:
  - default:
      memory: 1Gi
    defaultRequest:
      memory: 51Gim
    max:
      memory: 1Gi
    min:
      memory: 500Mi
    type: Container